"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const prefixes_1 = __importDefault(require("./prefixes"));
function getParts(prefixed) {
    const [prefix, term] = prefixed.split(':');
    if (!prefix || !term) {
        return null;
    }
    const baseIRI = prefixes_1.default[prefix];
    if (!baseIRI) {
        throw new Error(`Unavailable prefix '${prefix}:'`);
    }
    return { prefix, term, baseIRI };
}
exports.getParts = getParts;
function expand(prefixed) {
    const parts = getParts(prefixed);
    if (!parts) {
        return '';
    }
    return `${parts.baseIRI}${parts.term}`;
}
exports.expand = expand;
