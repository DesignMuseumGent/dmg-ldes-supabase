export declare class Bookkeeper {
    protected readonly queue: any;
    protected readonly queued: any;
    protected blacklist: Set<string>;
    constructor();
    fragmentAlreadyAdded(url: string): boolean;
    fragmentIsBlacklisted(url: string): boolean;
    blacklistFragment(url: string): void;
    addFragment(url: string, ttl: number): void;
    nextFragmentExists(): boolean;
    getNextFragmentToFetch(): FragmentInfo;
    serialize(): Object;
    deserialize(data: any): void;
    private serializePriorityQueue;
    private deserializePriorityQueue;
    inSyncingMode(): boolean;
}
interface FragmentInfo {
    url: string;
    refetchTime: Date;
}
export {};
