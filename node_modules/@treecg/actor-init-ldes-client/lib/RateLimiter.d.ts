export default class RateLimiter {
    readonly nextRequest: Record<string, number>;
    readonly minTime: number;
    constructor(minTime: number);
    planRequest(url: string): Promise<void>;
    protected sleep(ms: number): Promise<unknown>;
}
